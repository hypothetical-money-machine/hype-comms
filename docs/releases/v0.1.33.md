## Fixes

- This is a packaging-only cut. Production desktop packages now bake
  `HYPE_COMMS_API_ORIGIN=https://chat-api.hypemm.com` so first-run sign-in reaches the
  real chat API instead of the placeholder host from 0.1.32.
