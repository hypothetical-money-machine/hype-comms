# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "Hype Comms"

[setup]
script = """
set -eu

expected_node="$(cat .node-version)"
actual_node="$(node --version)"
if [ "$actual_node" != "v$expected_node" ]; then
  echo "Hype Comms requires Node v$expected_node; found $actual_node." >&2
  exit 1
fi

expected_npm="$(node -p \"require('./package.json').packageManager.split('@')[1]\")"
actual_npm="$(npm --version)"
if [ "$actual_npm" != "$expected_npm" ]; then
  echo "Hype Comms requires npm $expected_npm; found $actual_npm." >&2
  exit 1
fi

npm ci
"""

[[actions]]
name = "Run"
icon = "run"
command = "npm run demo"
